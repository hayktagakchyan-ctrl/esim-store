aiogram==3.14.0
SQLAlchemy==2.0.35
greenlet==3.1.1
aiosqlite==0.20.0
asyncpg==0.30.0
httpx==0.27.2
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9
itsdangerous==2.2.0
pydantic-settings==2.5.2

# greenlet — SQLAlchemy async вообще не может работать без него (любой драйвер,
# и SQLite, и Postgres). У тебя локально на Windows он, похоже, уже был установлен
# как побочный эффект от чего-то другого в venv, поэтому не бросалось в глаза,
# что он не прописан явно — а на чистой сборке Railway этого запасного
# экземпляра не было, отсюда и ValueError "greenlet library is required".

# asyncpg — драйвер Postgres для async SQLAlchemy. На Windows более старым версиям
# (включая 0.29.0) требовался установленный компилятор (Microsoft C++ Build Tools),
# из-за чего ты когда-то ловил ошибку установки — версия 0.30.0 уже с готовыми wheel
# и для Windows, но что важнее: на Railway сборка идёт на Linux, там таких проблем в
# принципе не бывает. Локально на SQLite он просто не используется — не мешает.
